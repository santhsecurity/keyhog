# Docs: https://hasura.io/docs/2.0/auth/authentication/admin-secret-access/
# Format: Admin secret configured via HASURA_GRAPHQL_ADMIN_SECRET env var, passed in X-Hasura-Admin-Secret header
# Verify: POST /v1/graphql with X-Hasura-Admin-Secret header returns 200 if valid
# Prefix: none (requires context anchoring)

[detector]
id = "hasura-admin-secret"
name = "Hasura Admin Secret"
service = "hasura"
severity = "critical"
keywords = ["HASURA_GRAPHQL_ADMIN_SECRET", "hasura", "X-Hasura-Admin-Secret"]

[[detector.patterns]]
regex = "(?:HASURA_GRAPHQL_ADMIN_SECRET|hasura.?admin)[=:\\s\"'']+([a-zA-Z0-9_-]{16,})"
description = "Hasura admin secret with context anchor"
group = 1

[[detector.patterns]]
regex = "X-Hasura-Admin-Secret:\\s*([a-zA-Z0-9_-]{8,})"
description = "Hasura admin secret in HTTP header"
group = 1
