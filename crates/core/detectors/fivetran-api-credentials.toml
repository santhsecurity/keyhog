# Docs: https://fivetran.com/docs/rest-api/getting-started
# Format: API Key and API Secret used with Basic authentication (key:secret base64 encoded)
# Verify: GET /v1/groups with Basic auth returns 200 if valid
# Prefix: none (requires context anchoring)

[detector]
id = "fivetran-api-credentials"
name = "Fivetran API Credentials"
service = "fivetran"
severity = "critical"
keywords = ["FIVETRAN", "fivetran", "FIVETRAN_API_KEY", "FIVETRAN_API_SECRET"]

[[detector.patterns]]
regex = "(?:FIVETRAN)[_.]?API[._-]?KEY[=:\\s\"'']+([a-zA-Z0-9]{20,})"
description = "Fivetran API key with context anchor"
group = 1

[[detector.patterns]]
regex = "(?:fivetran)[_.]?api[._-]?key[=:\\s\"'']+([a-zA-Z0-9]{20,})"
description = "Fivetran API key lowercase context anchor"
group = 1

[detector.verify]
method = "GET"
url = "https://api.fivetran.com/v1/groups"

[detector.verify.auth]
type = "basic"
username = "match"
password = ""

[detector.verify.success]
status = 200
