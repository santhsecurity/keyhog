# Docs: https://buildkite.com/docs/apis/managing-api-tokens
# Format: bkua_ prefix followed by 32 alphanumeric characters
# Verify: GET request to Buildkite user endpoint with Bearer token
# Prefix: bkua_

[detector]
id = "buildkite-api-access-token"
name = "Buildkite API Access Token"
service = "buildkite"
severity = "critical"
keywords = ["bkua_", "buildkite"]

[[detector.patterns]]
regex = 'bkua_[a-zA-Z0-9]{32}'
description = "Buildkite API access token (bkua_ prefix + 32 chars)"

[detector.verify]
method = "GET"
url = "https://api.buildkite.com/v2/user"

[detector.verify.auth]
type = "bearer"
field = "match"

[detector.verify.success]
status = 200

[[detector.verify.metadata]]
name = "name"
json_path = "name"
